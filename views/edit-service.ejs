<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Service</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background-color: #f8f9fa;
    }
    .edit-form-container {
      background: white;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
    }
    .form-title {
      border-left: 5px solid #007bff;
      padding-left: 15px;
      margin-bottom: 25px;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <div class="edit-form-container">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="form-title">Edit Service</h2>
        <a href="/admin/dashboard" class="btn btn-outline-secondary">← Back to Dashboard</a>
      </div>

      <form action="/admin/edit/<%= service._id %>" method="POST" enctype="multipart/form-data">
        <div class="mb-3">
          <label for="title" class="form-label">Service Title</label>
          <input type="text" class="form-control" name="title" id="title" value="<%= service.title %>" required>
        </div>

        <div class="mb-3">
          <label for="category" class="form-label">Service Category</label>
          <select name="category" id="category" class="form-select" required>
            <option disabled>Select Category</option>
            <option value="Analysis of Chemicals, Herbs, and Food and Beverage Products" <%= service.category === "Analysis of Chemicals, Herbs, and Food and Beverage Products" ? 'selected' : '' %>>Analysis of Chemicals, Herbs, and Food and Beverage Products</option>
            <option value="Isolation and Procurement Services" <%= service.category === "Isolation and Procurement Services" ? 'selected' : '' %>>Isolation and Procurement Services</option>
            <option value="Training and Advising for Scholars" <%= service.category === "Training and Advising for Scholars" ? 'selected' : '' %>>Training and Advising for Scholars</option>
            <option value="Scientific Writing Services" <%= service.category === "Scientific Writing Services" ? 'selected' : '' %>>Scientific Writing Services</option>
          </select>
        </div>

        <div class="mb-4">
          <label for="image" class="form-label">Replace Image (optional)</label>
          <input type="file" name="image" class="form-control" accept="image/*">
          <small class="form-text text-muted">Current image shown below:</small>
          <div class="mt-3">
            <img src="<%= service.image %>" alt="Current Image" class="img-thumbnail" style="height: 150px;">
          </div>
        </div>

        <button type="submit" class="btn btn-primary w-100">Update Service</button>
      </form>
    </div>
  </div>
</body>
</html>
